[tool.uv]
package = false

[tool.uv.workspace]
members = ["server", "app"]

[tool.pytest.ini_options]
pythonpath = [".", "app"]
asyncio_mode = "auto"

[dependency-groups]
dev = [
    "pytest>=9.1.1",
    "pytest-asyncio>=1.4.0",
    "pytest-mock>=3.15.1",
]

